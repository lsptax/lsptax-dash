# Backend API base URL (no trailing slash).
# In development: Vite proxies /api, /auth, /invoice, /action to this URL to avoid CORS.
# In production: the app sends requests directly to this URL (ensure backend allows CORS or is same-origin).
VITE_BACKEND_URL=http://localhost:3000
